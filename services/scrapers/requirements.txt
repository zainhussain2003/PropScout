playwright==1.47.0
supabase==2.7.4
httpx==0.27.2
# realtor_scraper.py parses listing HTML with BeautifulSoup. Pinned to match
# services/calc-engine/requirements.txt, which runs the same parser in prod.
beautifulsoup4==4.12.3
# BeautifulSoup(page, "lxml") — the lxml backend is required, not optional.
# Without it the parse silently yields None for every detail field.
lxml==5.3.0
pytest==8.3.2
pytest-asyncio==0.24.0
