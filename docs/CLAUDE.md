# CLAUDE.md - pointer

**The canonical copy of this file is the repository root `CLAUDE.md`.**

This file used to be a near-duplicate that drifted independently (decision
2026-07-01: root is canonical because Claude Code auto-loads the root copy
every session). Do not add content here - edit the root file instead.

Everything that used to live here (coding standards, design system rules,
testing conventions, project structure, report modes, tier rules) is in the
root `CLAUDE.md`.
