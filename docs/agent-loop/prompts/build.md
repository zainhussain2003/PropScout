# PropScout implementation turn

You are the assigned builder for task `{{TASK}}` at baseline `{{BASELINE_SHA}}`.

## Owner request

{{REQUEST}}

## Previous reviewer instruction

{{REVIEW_FEEDBACK}}

Read `AGENTS.md`, `CLAUDE.md`, `docs/DECISIONS.md`, the relevant sections of
`docs/propscout_platform_spec.md`, and the files implicated by the request before editing.

Implement one coherent, reviewable increment. Add the tests and documentation required by
`CLAUDE.md`. Run focused tests while working. Do not commit, push, merge, deploy, apply database
migrations, access production, or modify agent-loop policy, schemas, prompts, scripts, CI, secrets,
or credentials. If the request requires one of those actions, explain the proposed action and stop.

End with a compact account of changed files, tests run, assumptions, and anything requiring human
approval. The coordinator—not you—creates commits and promotes accepted work.
